{
    "type": "object",
    "properties": {
        "needs_policy": {"type": "boolean"},
        "classifier_confidence": {"type": "number", "minimum": 0.0, "maximum": 1.0},
        "domains": {
            "type": "array",
            "items": {
                "type": "string",
                "enum": ["software", "infrastructure", "shopping", "travel", "finance", "career", "health", "communication", "productivity", "general"]
            }
        },
        "interaction_mode": {
            "type": "string",
            "enum": ["recommend", "compare", "decide", "plan", "learn", "review", "brainstorm", "troubleshoot", "other"]
        }
    },
    "required": ["needs_policy", "classifier_confidence", "domains", "interaction_mode"]
}
